# Live API Agent

This is a prototype ADK agent for looking up product details and SKUs on the fly.

## Getting Started

To test this agent locally with the ADK Web UI, run the following commands:

```bash
agents-cli install
agents-cli playground
```

Once the UI is running, you can verify the agent by typing:
> "hello, what can you help me with?"
